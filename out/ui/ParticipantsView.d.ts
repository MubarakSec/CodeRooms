import * as vscode from 'vscode';
import { RoomState } from '../core/RoomState';
import { DocumentSync } from '../core/DocumentSync';
import { SuggestionManager } from '../core/SuggestionManager';
import { FollowController } from '../core/FollowController';
export declare class ParticipantsView implements vscode.TreeDataProvider<vscode.TreeItem> {
    private readonly roomState;
    private readonly documentSync;
    private readonly suggestionManager;
    private readonly followController;
    private readonly emitter;
    readonly onDidChangeTreeData: vscode.Event<vscode.TreeItem | undefined>;
    private readonly treeViews;
    constructor(roomState: RoomState, documentSync: DocumentSync, suggestionManager: SuggestionManager, followController: FollowController);
    refresh(): void;
    registerTreeView(viewId: string, treeView: vscode.TreeView<vscode.TreeItem>): void;
    reveal(preferredView?: string): Promise<void>;
    getTreeItem(element: vscode.TreeItem): vscode.TreeItem;
    getChildren(element?: vscode.TreeItem): Promise<vscode.TreeItem[]>;
    private buildRootBlocks;
    private createSessionHeader;
    private buildSessionBlock;
    private createWorkHeader;
    private buildWorkBlock;
    private createPeopleHeader;
    private buildPeopleBlock;
    private countRoles;
    private createSuggestionsHeader;
    private buildSuggestionsBlock;
}
